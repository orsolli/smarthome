# TODO

## Bugs to fix
- [ ] Fix `TreeFormatterImpl` to use consistent tree symbols in deep trees (currently causing `test_formatter_deep_tree` to fail)

## Improvements
- [ ] Implement more comprehensive integration tests
- [ ] Add error handling for malformed input trees
- [ ] Implement a better logger using the `logging` module
- [ ] Add more utility functions for tree manipulation
- [ ] Fix the typo in `tree_parser/__init__.py` where `TreeMergerImpl` is imported as `TreeMermerImpl` (from git diff analysis)
